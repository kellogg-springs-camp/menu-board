<div class="menu-board">
  <header>
    <div id="menu-head">
      <h1><img src="./public/KSClogo-nobottomwords.png" alt="Kellogg Springs Camp" />Next Menu</h1>
    </div>
  </header>
  <section class="menu_items">
    <ul>
      {{#each menuItems}}
        <li>{{getFKNameFromId food-item_id ../foodItems}}</li>
      {{/each}}
    </ul>
  </section>
</div>